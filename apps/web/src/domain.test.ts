import { describe,expect,it } from 'vitest';import { canAccessRole,formatQuantity } from './domain';
describe('frontend domain rules',()=>{it('applies role menu boundaries',()=>{expect(canAccessRole(['ADMIN','WAREHOUSE'],'WAREHOUSE')).toBe(true);expect(canAccessRole(['ADMIN','WAREHOUSE'],'PRODUCTION')).toBe(false);});it('formats all business quantities as integers',()=>{expect(formatQuantity('90.0000')).toBe('90');expect(formatQuantity('1.25')).toBe('1');});});
